/* @Module: APP
 * The public interface module organized into various,
 * loosely-coupled, augmentable sub-modules.
 *
 * @param: NONE
 * @return: public interface
 */
var APP = (function () {
    var app = {}; // public interface to return

        // Add methods like so: app.method = function () {};

    return app;
}());
